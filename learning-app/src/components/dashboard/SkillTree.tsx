import React from 'react';
import { Box, Typography } from '@mui/material';

const SkillTree: React.FC = () => {
    return (
        <Box>
            <Typography variant="h4" gutterBottom>
                Skill Tree
            </Typography>
            {/* Add your skill tree visualization logic here */}
        </Box>
    );
};

export default SkillTree;